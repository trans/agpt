# Logs note

The `/tmp/dr_*.log` files generated during the live experiment session
were lost on reboot before this directory was created. Running the
`run_*.sh` scripts here will regenerate them in this `logs/` directory.

The result tables in `../README.md` and `../findings.md` are the
preserved record of what those logs contained.
