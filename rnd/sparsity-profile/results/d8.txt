Loading radix trie: /tmp/agpt_input_d8_radix

==============================================================================
TRIE SIZE
==============================================================================
  radix_count        = 845540
    Number of nodes AFTER radix compression (storage units in this file).
    Each radix node has an 'edge' of 1+ tokens that represents a unary
    chain collapsed from the uncompressed trie.
    (Includes the virtual root at id=0, edge_len=0.)

  total_edge_chars   = 1545081
    Sum of edge lengths across all radix nodes. Equivalent to the node
    count of the UNCOMPRESSED (leveled) D-trie — i.e., the number of
    distinct (path-from-root, token) character positions the corpus
    produced.

  compression        = 1.83x
    = total_edge_chars / radix_count. How many leveled-trie nodes, on
    average, each radix storage unit represents.

  depth_file_count   = 9 (= D+1, files are indexed 0..D where D = cap depth)
  vocab_size         = 65
  corpus_token_count = 1115394

==============================================================================
PER-DEPTH PROFILE
==============================================================================
  depth          Endpoint depth of radix nodes (where their edges end).
                 d=1..D-1 = interior branching points.
                 d=D     = CAP — the max depth, where long unary tails terminate.

  n_nodes        Number of radix endpoints at this depth.

  total_count    Σ counts_val across those endpoints = total corpus mass
                 that hits a branching decision at this depth.
                 (A corpus D-gram branches multiple times on its way to
                 the cap; it contributes to several depths' total_count.)

  median_cnt     Median / mean / max of the per-endpoint total count
  mean_cnt       at this depth (how many corpus positions passed through
  max_cnt        each endpoint at this depth).

  avg_branch     Mean branching factor = mean counts.size per endpoint.
                 At interior depths ≥ 2 by construction (radix collapses
                 unary chains, so only real branching points survive as
                 interior endpoints). At the cap, can be 1 (singleton).

  avg_edge_len   Mean length of the unary-compressed edge ending at this
                 endpoint. 1 = no compression; larger = longer unary
                 chain got absorbed into this edge.

  chars_absorbed Σ (edge_len - 1) at this depth = # of leveled-trie nodes
                 that got collapsed INTO edges ending at this depth.

  %singleton     At the cap only: fraction of endpoints with counts.size==1
                 (i.e., unique corpus D-grams with a single observed
                 continuation). Interior depths are tautologically 0% and
                 shown as (n/a).

  depth  n_nodes    total_count  median_cnt  mean_cnt   max_cnt  avg_branch  avg_edge_len  chars_absorbed  %singleton
      1         62        1114756        6187   17979.94     169892      22.58          1.00               0     (n/a)
      2       1139        1102212         181     967.70      27643       9.91          1.00               1     (n/a)
      3       7624        1059288          26     138.94      16032       6.14          1.01              76     (n/a)
      4      25187         968778           9      38.46       8684       4.59          1.03             812     (n/a)
      5      51508         843633           5      16.38       5261       3.76          1.09            4806     (n/a)
      6      71524         688620           4       9.63       1737       3.29          1.21           14801     (n/a)
      7      78836         533327           3       6.77        684       3.06          1.38           30048     (n/a)
      8     609659        1115386           1       1.83        337       1.23          2.06          648998  87.55%

==============================================================================
COMPRESSION SUMMARY
==============================================================================
  leveled trie node count  = 1545081  (= total_edge_chars)
  radix endpoints          = 845539  (= storage units with an edge)
  absorbed into edges      = 699542  (= leveled nodes hidden inside radix edges)
                             45.28% of leveled nodes got compressed
  compression ratio        = 1.83x  (avg leveled nodes per radix storage unit)
